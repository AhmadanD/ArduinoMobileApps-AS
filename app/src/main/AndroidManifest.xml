<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arduinomobileapps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArduinoMobileApps">
        <activity
            android:name=".HalamanUtama"
            android:label="@string/title_activity_halaman_utama"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ArduinoMobileApps.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListData"
            android:label="@string/title_activity_list_data"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ArduinoMobileApps.NoActionBar">
        </activity>

    </application>

</manifest>